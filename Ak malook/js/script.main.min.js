var menu = document.getElementById('MenuIcon');
var icon = document.getElementById('menuToggle');

icon.addEventListener('click',(event)=>{
    menu.classList.toggle('hide')
    event.stopPropagation()
})

document.addEventListener('click',(event)=>{
    if(menu.classList.contains('hide')){
        menu.classList.remove('hide')
    }
    event.stopPropagation()
})

// ScrollReveal
// const sr = ScrollReveal({
//     origin: 'top',
//     distance: '80px',
//     duration: 1000,
//     reset: false
// });

// sr.reveal('.hero-content h1');
// sr.reveal('.hero-content p');
// sr.reveal('.hero-content .btn');

 ScrollReveal({
    reset: false,
    distance: '80px',
    duration: 2000,
    delay: 200
 });

 ScrollReveal().reveal('.text-box, .Ex-Our-Ser, .text-icon, .Our-Project, .Photo-logos', { origin: 'top' });
 ScrollReveal().reveal('.vm-container, .service-icon, .services-grid, .projects-grid, .ceo-content h4', { origin: 'bottom' });
ScrollReveal().reveal('.ceo-image', { origin: 'left' });
ScrollReveal().reveal('.about-image, .ceo-content', { easing: 'ease-out'  });


  const scrollBtn = document.getElementById("scrollTopBtn");

  window.addEventListener("scroll", () => {
    if (window.scrollY > 300) {
      scrollBtn.classList.add("show");
    } else {
      scrollBtn.classList.remove("show");
    }
  });

  scrollBtn.addEventListener("click", () => {
    window.scrollTo({
      top: 0,
      behavior: "smooth"
    });
  });
